# 验证记录

日期：2026-09-24

## 当前环境

- 操作系统：Windows。
- 未发现 Xcode、xcodebuild、xcrun 或 Swift 编译器。
- 未取得可用 Mac 构建环境及苹果 Team 签名配置。

## 验证范围

- 已通过 OpenStep 解析器验证 project.pbxproj 语法。
- 已验证全部工程对象引用、源文件路径、构建阶段与共享 Scheme 的目标引用。
- 已解析 Info.plist、workspace XML、Scheme XML 和资源 JSON。
- 已检查 App 图标为 1024 × 1024、RGB、无透明通道的 PNG。
- 已检查文本为 UTF-8、LF 换行；Mac 脚本不含 Windows CRLF。
- 已验证交付 ZIP 的 CRC 及每个文件的字节与源码一致；不包含缓存、账号、证书或私钥。

## 尚未验证

- Swift 源码在苹果 SDK 下的编译。
- iOS 模拟器实际运行与界面外观。
- 真机签名、安装、点击交互与离线运行。
- IPA 导出及签名有效性。

当前没有 IPA 或已构建的 .app；静态配置检查不能替代 Xcode 构建与真机测试。
